# Pass Statement

## What I Learned

The pass statement is used as a placeholder.

Example:

```python
if True:
    pass
```

It allows the program to run even when no code has been written inside a block yet.
