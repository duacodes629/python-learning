# Selection (if, elif, else)

## What I Learned

Selection allows Python to make decisions.

Example:

```python
age = 19

if age >= 18:
    print("Adult")
else:
    print("Child")
```

I learned:

- if
- elif
- else
